<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Add Feed</title>
  <div class="h2">
    Feed Builder
    <span class="glyphicon glyphicon-wrench"></span>
  </div>
</head>
<body>
<div class="authform pull-left">
    <%= form_for @add_feed, url: {:action => "create"} do |f| %>
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, :autofocus => true, class: 'form-control'  %>
          </div>
          <div class="form-group">
            <%= f.label :url %>
            <%= f.text_field :url, :autofocus => true, class: 'form-control'  %>
          </div>
          <div class="form-group">
            <%= f.label :tag_list %>
            <%= f.text_field :tag_list, :autofocus => true, class: 'form-control'  %>
          </div>
            <%= f.submit :class => "btn btn-primary" %>
    <% end %>
</div>
</body>
</html>