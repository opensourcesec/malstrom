<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Feeds</title>
  <h4>
    <%= image_tag("rss_cube.png", :size => "65x35", :style => "float :left",:crop => :fill) %>
    Add Threat Feed <%= link_to '', feeds_new_feed_path, class: 'glyphicon glyphicon-plus btn btn-success' %>
  </h4>
</head>
  <body>
    <div class="col-xs-12">
    <div class="panel panel-success">
    <table id="feed_table" class="table table-striped">
    <thead>
    <tr>
        <th>Name</th>
        <th>URL</th>
        <th>Run Now</th>
        <th>Scheduler</th>
        <th>Remove</th>
        <th>Tags</th>
    </tr>
    </thead>
    <tbody>
        <% @feeds.each do |feed| %>
        <tr>
            <td><%= feed.name %></td>
            <td><%= feed.url %></td>
            <td><%= link_to '', feeds_run_feed_path(:url => feed.url, :name => feed.name, :tags => feed.tag_list), class: 'glyphicon glyphicon-retweet btn btn-primary' %></td>
            <td><label class="switch-toggle well">
              <input type="checkbox">
              <span>
              <span>Off</span>
              <span>On</span>
              </span>
              <a class="btn btn-success active"></a>
            </label></td>
            <td><%= link_to '', feeds_delete_feed_path(:name => feed.name), class: 'glyphicon glyphicon-trash btn btn-danger' %></td>
            <% feed.tag_list.each do |tag| %>
            <td><%= link_to tag, threats_profile_path(:tag => tag), :class => 'label label-large label-info' %></td>
            <% end %>
        </tr>
        <% end %>
    </tbody>
    </table>
    </div>
    </div>
  </body>
</html>
