<% if user_signed_in? %>
    <script>
        $('dropdown-toggle').dropdown()
    </script>
    <li class="dropdown">
      <a href="#" data-toggle="dropdown" class="dropdown-toggle">Help <b class="caret"></b></a>
      <ul class="dropdown-menu">
        <li><%= link_to " About", "#testModal", :class => "glyphicon glyphicon-question-sign", "data-toggle" => "modal" %></li>
        <li class="divider"></li>
        <li><%= link_to " Docs", "#", :class => "glyphicon glyphicon-question-sign", "data-toggle" => "modal" %></li>
      </ul>
    </li>
    <script>
        $('#element').tooltip('show')
    </script>
    <li><%= link_to '', edit_user_registration_path, :class => 'navbar-brand glyphicon glyphicon-user', :data => {:toggle=>"tooltip"}, :title => "Edit Account" %></li>
    <li><%= button_to 'Logout', destroy_user_session_path, :method=>'delete', :class => 'btn btn-default navbar-btn' %></li>
<% else %>
<% end %>